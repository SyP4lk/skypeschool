import React from 'react';export function Card({className='',children}:{className?:string;children:React.ReactNode}){return <section className={'border rounded-xl p-4 bg-white shadow-sm '+className}>{children}</section>;}export function CardTitle({className='',children}:{className?:string;children:React.ReactNode}){return <h2 className={'text-lg font-semibold mb-3 '+className}>{children}</h2>;}export default Card;